name: Tests

on:
  push:
    branches: [ main, develop ]
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - 'LICENSE'
  pull_request:
    branches: [ main, develop ]
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - 'LICENSE'

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run linter
      run: npm run lint --if-present

    - name: Run type check
      run: npm run build

    - name: Run tests
      run: npm test

    - name: Run test coverage
      run: npm run test:coverage --if-present

    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      if: matrix.node-version == '20.x'
      with:
        files: ./coverage/coverage-final.json
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Check code formatting
      run: npm run format --if-present -- --check

    - name: Run ESLint
      run: npm run lint --if-present

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build TypeScript
      run: npm run build

    - name: Check for build errors
      run: |
        if [ -d "dist" ]; then
          echo "Build successful"
        else
          echo "Build failed - dist directory not created"
          exit 1
        fi

  # E2E Tests with Playwright (temporarily disabled - requires stable D1 state)
  # TODO: Re-enable once D1 state persistence is fixed in CI
  # e2e:
  #   runs-on: ubuntu-latest
  #   needs: [test, lint, build]
  # TODO: Re-enable once D1 state persistence is fixed in CI
  # e2e:
  #   runs-on: ubuntu-latest
  #   needs: [test, lint, build]
  #   
  #   steps:
  #   - uses: actions/checkout@v4
  #   
  #   - name: Use Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: '20.x'
  #       cache: 'npm'
  #   
  #   - name: Install dependencies
  #     run: npm ci
  #   
  #   - name: Install Playwright Browsers
  #     run: npx playwright install --with-deps chromium
  #   
  #   - name: Setup D1 database and apply migrations
  #     run: |
  #       # Create local D1 instance and apply migrations
  #       npx wrangler d1 migrations apply DB --local --persist-to=.wrangler/state/v3
  #       echo "✅ Migrations applied"
  #   
  #   - name: Run E2E tests
  #     run: npm run test:e2e
  #   
  #   - name: Upload test results
  #     if: always()
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: playwright-report
  #       path: playwright-report/
  #       retention-days: 30
  #   
  #   - name: Upload screenshots
  #     if: always()
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: test-screenshots
  #       path: test-results/screenshots/
  #       retention-days: 30
  #   
  #   - name: Upload videos
  #     if: failure()
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: test-videos
  #       path: test-results/
  #       retention-days: 30
